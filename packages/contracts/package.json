{
  "name": "@app/contracts",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "prebuild": "rimraf dist",
    "lint": "eslint \"src/**/*.ts\" --fix",
    "build:tsc-only": "tsc --noEmit"
  },
  "devDependencies": {
    "eslint-config-custom": "*",
    "rimraf": "^6.1.3",
    "tsconfig": "*",
    "typescript": "5.9.3"
  },
  "lint-staged": {
    "*.(js|ts)": "eslint --fix",
    "*.(md|json)": "prettier --write"
  },
  "files": [
    "dist"
  ]
}
